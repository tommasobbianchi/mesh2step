# Action plan — 2026-09-10, after the DeepSeek continuation

## Where we actually are (read from disk, not from memory)

**DeepSeek delivered the result the campaign was blocked on.** Branch `n6-oracle-test`
(head `0bac065`), claimed in the commit bodies:

| gate | claim |
|---|---|
| NORMAL 57 | **87 → 277 cylinders (+190, 0 worse)** — SpeedTest 0→178, soap_bar 0→12 |
| NEG FINE 49 | 102 → 114 (+12, 0 worse) — SuperSOT-3 0→12 |
| sentinels | SC-59 / SOT-143 / TSOT-23 **restored to 12 / 16 / 12** |
| ISLE_CAP default-on | L10_cross_slide 0→23 |
| post-write guard | rc=1 + output removed on writer-break; 0 false positives on 111 models |
| known | 5 DIN41612 connectors time out on BOTH arms (pre-existing) |

This is the same +190 that was HELD on 2026-09-07 because it destroyed 40 CAD-verified
cylinders. The sentinel restoration is what changed. If it verifies, corpus recall goes
**18.4 % → ~58 %** and it is the promotion of the campaign.

**Live is untouched and healthy:** `mesh2step.service` active since the 08:41 reboot,
`NRestarts=0`, MemoryMax 24 G, engine still **v1.1.0-n5h**, answering 200. Nothing DeepSeek did
has shipped.

## The blocker, and it is not the engine

**The box rebooted at 08:41 today and `/tmp` was cleared.** Gone: the 333-model corpus
(`/tmp/awk/cadbench/*.stl`), **`truth.json`**, every scratch harness (`sweep.sh`, `gate2b.py`,
`rungate.sh`, `assert_twin_idle.sh`, the P0 scripts), `/tmp/n4-handover.md`, the reports and all
checkpoints. Survived only because they were copied out: `~/.cache/n4/HANDOVER-FINAL-2026-09-08.md`.

So **every number above is currently unverifiable**, and the standing rule is parity before
belief. Worse, the same loss will happen at the next reboot.

Recoverable, checked: the alphaevolve benchmark (`~/projects/cad-3d/reverse-engineering/`
`alphaevolve/benchmark/` — `step/`, `reconstructed/` 200 files, `manifest.json`) and the KiCad
Packages3D flatpak runtime both survive. **No generator script ever existed** — the corpus was
built ad hoc, which is why one reboot destroyed it.

---

## Plan

### 1. Rebuild the corpus, durably, with a committed generator  (blocks everything)

Write `scripts/build_corpus.py` in the repo (tracked, not scratch) that regenerates from the
surviving sources into **`~/corpora/cadbench/`**, never `/tmp`:

- tessellate each benchmark STEP at the three deflections; the surviving sample shows they are
  proportional to the model diagonal (fine 0.0005·diag, normal 0.001·diag, coarse 0.00333·diag —
  0.00149 / 0.00297 / 0.0099 on a 2.97 mm diag). Verify, do not assume;
- rebuild `truth.json` by **counting faces on the source B-Rep with OCP** (`cyl_faces`,
  `planar_faces`, `other_faces`, `circle_edges`, `cyl_radii`). This is strictly better than what
  was lost: derived from CAD, reproducible, not hand-maintained;
- regenerate the 54 `NEG_*_fine.stl` from the KiCad runtime.

**Acceptance — anchors recorded before the loss, so this is a real test, not a vibe check:**
`SpeedTestStructure_normal` = 5,964 tris / 298,284 bytes · `sphere6` = 81,920 tris / 4,096,084 B ·
`mmx` = 64,288 tris / 3,214,484 B · `L06_adapter_plate_normal` = 908 tris ·
truth totals = 57 models, 41 with cylinders, **474** truth cylinders, SpeedTestStructure 246,
gripper_gear 75. If the totals do not reproduce, the regenerated corpus is a NEW baseline and
every historical number must be re-derived on it — say so rather than quietly comparing.

Also restore the harness (`sweep.sh`, `rungate.sh`, `gate2b.py`, `assert_twin_idle.sh`) into
`scripts/` under git, since scratch has proven not to survive.

### 2. Verify DeepSeek's claims independently  (parity before belief)

Build `n6-oracle-test` @ `0bac065` with the recorded recipe (project `FindOpenCASCADE` via
`CASROOT`, **never** `-DOpenCASCADE_DIR`), install sha-named, then:

- parity control first: the new binary with its features OFF must reproduce v1.1.0-n5h
  byte-for-byte on `L06_adapter_plate_normal` and `NEG_SC-59_fine` (DATA section only — the
  header carries a timestamp);
- full A/B on the restored corpus, parallel via `sweep.sh`, unit = model × arm;
- report: recall, **models worse (must be 0)**, sentinels 12/16/12, geometric equivalence where
  bytes differ, and the 5 DIN41612 timeouts on both arms.

I will not treat a commit message as a measurement.

### 3. Promotion decision, prepared but not taken

If §2 reproduces: run the full n5d-class gate (`MISSED_silent_wrong_rebuild = 0` on the negatives,
§2b v2 canonical loader, models-worse = 0 on the full set, recall strictly up) and present the
verdict. **Promotion and any live restart stay Tommaso's call** — announced, single owner,
logged, per the production rule.

### 4. The two items that were decided and never done

- **Negatives manifest** `tests/data/negatives_manifest.json` — all 54 `NEG_*` with source path,
  k, engaged flag. Every MISSED gate cites "the 26 negatives", a set that has never been defined.
- **`smoothRevertedComponents` lies** (counts adopted-with-zero-cylinders as reverted; its own
  cause string says `counted-not-reverted`). Fix at source + all Python readers in one change,
  then re-run the P0 bucketing, which must reproduce 79 / 326 exactly.

### 5. Hand back to Tommaso

The mesh2step working tree is still uncommitted and **is what production runs** (admission
control, instruments, `intent.py`, tests). It needs his review and commit; I will not commit it.

---

## Order and why

1 before 2 because nothing is measurable without a corpus. 2 before 3 because the campaign's
biggest claim should not ship on a commit message. 4 is cheap and both items are already-taken
decisions. 5 is his.

**Out of scope this turn:** n5g floors, P81 tangency, P71, the GNN chapter, surge UX. The +190
promotion dominates all of them; adding tracks is how the last three days went to negative
results.
